# Android APK Fallback

Place a signed Android release here only if Anahata support chooses to offer direct APK installation.

Recommended files:

```text
anahata-latest.apk
anahata-latest.sha256
```

After publishing the APK, update the disabled APK link in `../index.html`.
